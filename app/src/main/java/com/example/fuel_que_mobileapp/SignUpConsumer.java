package com.example.fuel_que_mobileapp;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

public class SignUpConsumer extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_signup_consumer);

//        Spinner mySpinner = (Spinner) findViewById(R.id.spinner2);
//
//        ArrayAdapter<String> myAdapter = new ArrayAdapter<String>(SignUpConsumer.this,
//                android.R.layout.simple_expandable_list_item_1, getResources().getStringArray(R.array.spinner2));
//        myAdapter.setDropDownViewResource(android.R.layout.simple_spinner_dropdown_item);
//        mySpinner.setAdapter(myAdapter);
    }
}